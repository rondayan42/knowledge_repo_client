apiVersion: v1
kind: Service
metadata:
  name: knowledge-repo-client
  labels:
    app: knowledge-repo-client
spec:
  type: ClusterIP
  selector:
    app: knowledge-repo-client
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
